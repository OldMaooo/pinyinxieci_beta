# Supabase 环境变量配置模板
# 
# 使用方法：
# 1. 复制此文件为 .env.local（已在 .gitignore 中，不会被提交）
# 2. 取消注释并填入对应的值
# 3. 本地开发时会自动读取 .env.local 中的配置
#
# 注意：Vite 要求环境变量必须以 VITE_ 开头才能在客户端使用

# ============================================
# 本地开发配置（推荐使用 Beta 数据库）
# ============================================
# VITE_SUPABASE_URL=https://your-beta-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-beta-anon-key-here

# ============================================
# 正式数据库配置（默认值，代码中已硬编码）
# ============================================
# 如果 .env.local 不存在或未设置，代码会使用以下默认值：
# VITE_SUPABASE_URL=https://ynasoxvdalcmrrsxxmjr.supabase.co
# VITE_SUPABASE_ANON_KEY=sb_publishable_ap2IKSLCxabTzVTQNbw45Q_iFBUaNJW
#
# 注意：Vercel 部署时会在项目设置中配置环境变量，不需要在这里设置
